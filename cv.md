First name: Darya Last name: ***
Contacts: ***
I'm tired of banal phrases that I strive to develop myself, so... I just want to try JS and RS (*priority is hidden here).
I'm aldo taking a course of Python now.
My last task from the Python course as an example of code:
====
def find_all(target, symbol):
    result = []
    if symbol in target:
        l = list(target)
        for i in range(len(l)):
            if l[i] == symbol:
                result.append(i)
        return result
    else:
        return result
        
s = input()
char = input()

print(find_all(s, char))
====
I don't have any experience of working as a progemmer.
I have a liberal arts background.
I'm taking an English course now (pre-intermediate degree).